import { useMediaQuery } from '@mui/material'
import React from 'react'

const ScreenSizes = () => {
  const smScreen = useMediaQuery('max-width : 479px')
  const mdScreen = useMedia
}

export default ScreenSizes